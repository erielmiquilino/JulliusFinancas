version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: postgres_server
    environment:
      POSTGRES_USER: jullius  # Substitua pelo nome de usuário desejado
      POSTGRES_PASSWORD: P@ssw0rd # Substitua pela senha desejada
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
